# mkspiffs is a build tool component
# It's not used as a runtime component in the application
# This file is here to prevent CMake errors

# Register as an interface component (no sources)
idf_component_register()
